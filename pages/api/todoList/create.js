// export default function handler(req, res) {
//   if (req.method !== "POST") {
//     req.status(400).json({ message: "Invalid Request Type" });
//   }

//   req.status(200).json({ name: "Post Create api" });
// }
